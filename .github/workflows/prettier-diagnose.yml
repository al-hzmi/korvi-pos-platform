name: Prettier Diagnostic Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Temporary diagnostic workflow disabled pending deletion"
