# Korvi V1 Release Candidate — 2026-09-14

This note records the bounded release-candidate delta immediately before final gate reconciliation. It does not approve launch and does not substitute for any independent/Human, HSM, submission, or production-operations gate.

## Dependency-stability repair

Parent candidate `f771ef89179e0d26c9f443a52d902bc90399432c` exposed a repository-wide verification failure because `@korvi/api` still pinned `zod` `4.6.4` after `4.6.5` became the current production-stable patch.

The dependency-only repair commit `6d6a5c19a373f659fd07444af5817d0d7853b9e5` updates exactly:

- `apps/api/package.json`
- `package-lock.json`

The lockfile was regenerated by npm against `https://registry.npmjs.org` and independently guarded against the authoritative `zod@4.6.5` tarball/integrity metadata. No application, financial, inventory, tax, ZATCA, authorization, or schema logic was changed by that repair.

## Release proof rule

All automated evidence used for final release decisions must be attributable to the exact candidate SHA being evaluated. Prior green runs remain historical evidence only when the release gate requires exact-SHA proof.

Independent/Human gates must remain human and independent. Automated proof cannot self-approve those gates.

Production-operations readiness must remain fail-closed: staging/free infrastructure, synthetic evidence, or repo-only assertions do not constitute production HA, backup/restore, monitoring/on-call, secrets-management, or merchant field-validation evidence.
